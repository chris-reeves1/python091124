# write that returns the strength of a password. 

# class - for checking strength:
# returns:
- very strong
- strong
- moderate
- weak
- very weak

# length, special characters, numbers etc. (check current owasp advice)
# Check against a list of common passwords - if mathced = very weak. 

# database setup - store passwords + ratings. 

# loop to run the program, asks for input displays history + strengths. 

# High score on pylint. 

# Optional: Tests for core logic. (Extract the logic in seperate file for testing)